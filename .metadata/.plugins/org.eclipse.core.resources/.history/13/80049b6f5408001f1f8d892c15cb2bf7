package com.kh.FileEx;
/*
 Stream(스트림)
  데이터를 순서대로 읽거나 쓰는데 사용 
  예를들어 
  	 (영화나 음악 스트리밍을 할 때 3분이 완료인 동영상이나 음악에서
  	 현재 1/3 을 보고 있는 상황임에 불구하고 2:58초 내용이 갑자기 보이면 안되는 것처럼
  	 데이터는 무조건 순서대로 보여짐)
  
  
  InputStream
  	입력 스트림을 읽어오는데 사용
  	메서드 : read close
  BufferedStream(In, Out)
  	데이터를 효율적으로 읽고 쓸 수 있음(효과 같은 역할)
  
  */
public class Stream {

}
